# Usage
1. You need to get SDK includes from: https://github.com/SDmodding/SDK
2. Create folder where you gonna extract the SDK and inside that create folder with name SDK and extract it to that folder
3. After opening Base project you need to modify Include directions to the folder where you extracted SDK
4. If everything is done correctly you will be able to simply build the base and have dll ready to be injected to the game
